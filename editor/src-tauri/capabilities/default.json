{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum the Editor needs: an OS open dialog, a native confirmation for discarding unsaved chart edits, the loader and save commands, and the asset protocol whose scope starts empty and is widened only to the one audio file a Project resolves to.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-ask",
    "core:window:allow-set-title"
  ]
}
