"""Unit tests for the Analyzer's pure logic.

Nothing here loads a model, touches a GPU or decodes real audio: those need the full
environment and a real song, and they belong in the integration run rather than in a
suite that should stay fast and dependency-light.
"""
